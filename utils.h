#pragma once
#include "Rectangle.h"

/**
 * @brief In thông tin chu vi và diện tích của hình chữ nhật.
 * @param index Số thứ tự hình chữ nhật.
 * @param r Đối tượng Rectangle cần in.
 */
void printRectangle(int index, Rectangle& r);
